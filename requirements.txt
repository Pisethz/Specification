# Python packages (for spec.py)
psutil
colorama
py-cpuinfo
gputil

# (Optional) For best WiFi/Bluetooth info on Linux, install:
# sudo apt install network-manager bluez
# or
# sudo dnf install NetworkManager bluez

# No extra dependencies required for mobilespec.py (Android/Termux) 